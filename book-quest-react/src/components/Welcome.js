import React from 'react'

const Welcome = (props) => {
    return(
        <div id="welcome-container">
            <button>Sign Up Today!</button>
            <button>Login</button>
        </div>
    )
}

export default Welcome