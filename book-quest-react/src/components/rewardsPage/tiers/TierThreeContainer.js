import React, { Fragment } from 'react'

const TierThreeContainer = (props) => {

    return(
        <React.Fragment>
            <div>Tier Three Container</div>
        </React.Fragment>
    )
}

export default TierThreeContainer